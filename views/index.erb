<!DOCTYPE html>
<html>
  <head>
    <title>News Aggregator</title>
  </head>
  <body>
    <h1>Articles</h1>

    <a href="/articles/new">Add New Article</a>

    <ul>
      <% articles.each do |article_info| %>
        <li>
          <h4>
            <a href="<%= article_info["url"] %>" target="_blank"><%= article_info["title"] %></a>
          </h4>
          <p><%= article_info["description"] %></p>
        </li>
      <% end %>
    </ul>
  </body>
</html>
